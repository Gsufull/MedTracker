<Page x:Class="MedTracker.Views.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="{DynamicResource Admin_Title}">

    <Grid Background="{DynamicResource BgColor}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{DynamicResource Admin_Title}" Style="{DynamicResource PageTitleStyle}"/>

        <Border Grid.Row="1" Style="{DynamicResource CardStyle}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="{DynamicResource Admin_Users}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}" Margin="0,0,0,15"/>
                
                <DataGrid x:Name="DgUsers" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Admin_Username}" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="{DynamicResource Admin_Role}" Binding="{Binding Role}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
                    <Button Content="{DynamicResource Admin_DeleteUser}" Style="{DynamicResource DangerBtnStyle}" Click="BtnDeleteUser_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="{DynamicResource Admin_Logs}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}" Margin="0,0,0,10"/>
        
        <Border Grid.Row="3" Style="{DynamicResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="{DynamicResource Admin_RefreshLogs}" Style="{DynamicResource NeutralBtnStyle}" HorizontalAlignment="Right" Margin="0,0,0,10" Click="BtnRefreshLogs_Click"/>
                <ListBox Grid.Row="1" x:Name="ListLogs" Background="{DynamicResource InputBg}" Foreground="{DynamicResource InputText}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Padding="5"/>
            </Grid>
        </Border>
    </Grid>
</Page>
